function genrand (num: number, num2: number) {
    if (0 >= 0) {
    	
    } else {
    	
    }
}
input.onButtonPressed(Button.A, function () {
    radio.sendNumber(control.millis() % (input.acceleration(Dimension.X) + input.lightLevel()))
})
basic.forever(function () {
	
})
